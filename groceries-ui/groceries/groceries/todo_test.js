'use strict';

describe('groceriesApp module', function() {

	beforeEach(module('groceriesApp'));

	describe('view1 controller', function() {

		it('should ....', inject(function($controller) {
			// spec body
			var todoCtrl = $controller('GroceriesAppCtrl');
			expect(todoCtrl).toBeDefined();
		}));

	});
});